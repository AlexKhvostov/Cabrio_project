<?php
/**
 * AddCarForUserAction — бизнес-операция "добавить авто себе" для пользователя.
 *
 * Назначение:
 *   Добавляет автомобиль пользователю:
 *   — Если авто уже есть в базе как "визитка" (без владельца), назначает пользователя владельцем.
 *   — Если авто не найдено, создаёт новый автомобиль и назначает владельца.
 *   — Если авто уже принадлежит другому пользователю, возвращает ошибку.
 *
 * Зависимости:
 *   - Car (модель)
 *   - User (модель)
 *   - LinkUserCar (модель)
 *   - BusinessCard (модель, для поиска визитки)
 *   - Logger, ResponseHelper (утилиты)
 *
 * Пример использования:
 *   $result = AddCarForUserAction::handle($carData, $user_id);
 */
class AddCarForUserAction {
    /**
     * Основной метод бизнес-операции
     * @param array $carData — данные автомобиля (номер, марка, модель и т.д.)
     * @param int $user_id — id пользователя, который добавляет авто
     * @return array — результат (успех/ошибка, данные авто, сообщения)
     *
     * Логика:
     *   1. Поиск авто по уникальным данным (например, номеру)
     *   2. Если авто найдено и НЕТ владельца (owner_user_id пустой/null):
     *        — Назначить пользователя владельцем (owner_user_id = $user_id)
     *        — Изменить статус авто на "активный"
     *        — Вернуть успех и данные авто
     *   3. Если авто найдено и уже есть владелец (owner_user_id заполнен):
     *        — Вернуть ошибку: "Авто уже принадлежит другому пользователю"
     *   4. Если авто не найдено:
     *        — Создать новый авто, назначить владельца, статус "активный"
     *        — Вернуть успех и данные авто
     *   5. Логировать действия
     */
    public static function handle($carData, $user_id) {
        // 1. Поиск авто по уникальным данным (например, номеру)
        // 2. Если найдено и статус "визитка" — назначить владельца, обновить статус
        // 3. Если не найдено — создать новый авто, назначить владельца
        // 4. Если найдено и уже есть владелец — вернуть ошибку.
        // 5. Логировать действия
        // 6. Вернуть результат
    }
} 