// config/bot.config.ts

/**
 * Конфигурация шаблонов и включения/выключения уведомлений Telegram-бота CabrioRide.
 * Все тексты уведомлений централизованы и могут быть отредактированы без изменения кода.
 * Для каждого типа уведомления можно включить/отключить отправку (enabled) и задать шаблон текста (text).
 * В шаблонах можно использовать переменные в фигурных скобках (например, {user}, {car}, {reason}).
 */
export interface BotNotificationsConfig {
  /**
   * Личные уведомления пользователю (в личку)
   */
  user: {
    /**
     * Уведомления о модерации профиля
     */
    moderation: {
      enabled: boolean; // Включить/выключить уведомления о модерации
      approved: string; // Текст при одобрении профиля
      rejected: string; // Текст при отклонении профиля (используйте {reason})
      sent: string;     // Текст при отправке профиля на модерацию
    };
    /**
     * Уведомления о получении respect
     */
    respect: {
      // ⚠️ Это не флаг пользователя, а отдельная система баллов/лайков (не влияет на роль/статус)
      enabled: boolean; // Включить/выключить уведомления о respect
      received: string; // Текст уведомления о получении respect
    };
    /**
     * Уведомления о регистрации пользователя
     */
    registration: {
      enabled: boolean; // Включить/выключить уведомления о регистрации
      approved: string; // Текст при одобрении регистрации
      rejected: string; // Текст при отклонении регистрации (используйте {reason})
      sent: string;     // Текст при отправке регистрации на модерацию
      notRegistered: string; // Текст для незарегистрированных пользователей
    };
    // ... другие личные уведомления
  };
  /**
   * Уведомления в групповой чат клуба
   */
  group: {
    /**
     * Приветствие нового участника
     */
    newUser: {
      enabled: boolean; // Включить/выключить приветствие
      text: string;     // Текст приветствия (используйте {user})
    };
    /**
     * Уведомление о новом авто
     */
    newCar: {
      enabled: boolean; // Включить/выключить уведомление
      text: string;     // Текст уведомления (используйте {car})
    };
    // ... другие групповые уведомления
  };
  /**
   * Приветственное сообщение при /start
   */
  welcome: {
    enabled: boolean; // Включить/выключить welcome-сообщение
    text: string;     // Текст приветствия
  };
  // ... другие блоки
}

/**
 * Пример объекта конфигурации уведомлений Telegram-бота CabrioRide.
 * Все тексты можно редактировать, переменные в фигурных скобках будут подставлены автоматически.
 */
const botNotifications: BotNotificationsConfig = {
  user: {
    moderation: {
      enabled: true,
      approved: 'Ваш профиль одобрен! Добро пожаловать в CabrioRide 🚗',
      rejected: 'Ваш профиль отклонён. Причина: {reason}',
      sent: 'Ваш профиль отправлен на модерацию.',
    },
    respect: {
      // ⚠️ Это не флаг пользователя, а отдельная система баллов/лайков (не влияет на роль/статус)
      enabled: true,
      received: 'Вам начислен respect от участника клуба!',
    },
    registration: {
      enabled: true,
      approved: 'Ваш профиль одобрен! Добро пожаловать в CabrioRide 🚗',
      rejected: 'Ваш профиль отклонён. Причина: {reason}',
      sent: 'Ваш профиль отправлен на модерацию.',
      notRegistered: 'Добро пожаловать в CabrioRide! Для доступа к клубу и всем функциям, пожалуйста, зарегистрируйтесь в приложении.\n[Зарегистрироваться](https://app.cabrioride.ru/?register=1)'
    },
    // ... другие уведомления
  },
  group: {
    newUser: {
      enabled: true,
      text: 'В клубе новый участник: {user}',
    },
    newCar: {
      enabled: true,
      text: 'В клубе новое авто: {car}',
    },
    // ... другие групповые уведомления
  },
  welcome: {
    enabled: true,
    text: 'Привет! Я бот CabrioRide. Я умею проверять авто по фото или номеру, присылать уведомления и переводить тебя в приложение. Для начала — нажми кнопку ниже!',
  },
  // ... другие блоки
};

export default botNotifications; 